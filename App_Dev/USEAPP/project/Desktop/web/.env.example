# Example environment variables for production (Vercel Project Settings → Environment Variables)
# Do NOT put real secrets in this file.

DB_HOST=your-mysql-host.example.com
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=it322

# Optional override used by API URL helpers
API_BASE_URL=https://your-deployment.vercel.app/api
